

<div class="container">
  <h1>List#index</h1>
    <p>Find me in app/views/list/index.html.erb</p>

    <h3 class="mb-0">My Lists</h3>
      <%= link_to "Create a movie list, bro", new_list_path, class: "btn btn-flat" %>

    <ul>
      <% @lists.each do |list| %>
        <li> <%= link_to list.name, list_path(list) %></li>
      <% end %>
    </ul>

</div>
